# <Stage / Task> 方法调研与复用审查

## 审查范围

- stage/task：`<id>`
- 调研日期：`YYYY-MM-DD`
- 需要实现的 capabilities：`<list>`
- 本轮相对 R00/既有方法卡的新条件：`<data, interface, version, scope changes>`

## 项目存量与本地环境

记录实际搜索过的项目代码、脚本、配置、测试、已安装依赖及版本；没有命中时写明搜索范围，而不是只写“无”。

## 必读参考资料

列出完整阅读的方法卡、idea 规则和必要一手材料，并说明各自约束了什么。

## 实时一手调研

记录查询范围、检索日期、论文官方页面、官方仓库、release/changelog 与 license。搜索摘要不能作为最终证据。

## 候选比较

| Capability | Candidate | Route | Interface fit | Scientific fit | License | Scale/maintenance | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `<id>` | `<name>` | `<reuse/adapt/minimal_new/defer>` | `<assessment>` | `<assessment>` | `<verdict>` | `<assessment>` | `<shortlist/reject/select>` |

## 归档更新

列出新增或修订的方法卡、文献索引和对应 decision ID。只在摘要层面排除的搜索结果记录链接与排除理由。

## 选择与 adapter seam

说明最终选择、调用者需要知道的最小 interface、adapter 职责、不会由该 module 承担的项目责任，以及验证方式。

## 自建例外

若选择 `minimal_new`，记录所有成熟候选的不适配证据、最小范围、ceiling、替换条件、测试和人工批准；否则写 `not_applicable`。

## 未解决项与 Gate

- unresolved：`<list>`
- reuse_gate：`pass | fail`
- 对 T01+ 的影响：`<authorized | blocked>`

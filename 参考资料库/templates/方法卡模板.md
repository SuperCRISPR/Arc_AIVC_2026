# <方法名称>

## 稳定标识与项目角色

- method_id: `<stable_method_id>`
- 项目角色：`<candidate | baseline | reusable_module | prior | diagnostic | excluded>`
- 关联阶段：`<R00/S00/...>`
- 关联 reuse decision：`<stage-RD-capability>`
- 最后核验日期：`YYYY-MM-DD`

## 解决的问题

用简洁语言说明该方法的输入、输出、目标 estimand 或建模问题，以及它为何可能适用于本项目。

## 关键假设与失效条件

- `<assumption>`
- `<known failure mode>`

## 一手来源

- 论文题目：`<title>`
- DOI / 官方论文页：`<url>`
- 官方仓库：`<url or none>`
- 官方文档 / release：`<url or none>`
- 本地论文：`论文.pdf | not_archived`

## 实现与许可快照

- version / commit：`<exact value>`
- license：`<SPDX or exact upstream statement>`
- 维护状态：`<active | limited | archived | unknown>`
- 关键依赖：`<list>`
- 核验边界：版本、许可和维护状态在实际复用前必须于阶段 T00 刷新。

## 可复用 module 与 interface

| Module | Interface | 可直接复用内容 | 必要 adapter |
| --- | --- | --- | --- |
| `<name>` | `<inputs / outputs / invariants>` | `<scope>` | `<adaptation>` |

## 本项目适用边界

### 允许用途

- `<allowed use>`

### 不允许替代

- `<project-specific responsibility this method cannot replace>`

## 验证要求

- `<small fixture / property / OOD comparison / numerical check>`

## 与已有方法的关系

- `<supersedes / complements / conflicts with another card>`

## 未解决问题

- `<unresolved item or none>`

# Arc Virtual Cell Challenge 2026 技术路线与项目设计 v2

> 本文件是 v1(`技术路线与项目设计.md`)的修订版,面向**实现**——即可以直接交给下游 AI coding agent 用来搭建代码库、设计工作流、写测试。v1 → v2 的具体变更理由见 `审核意见.md`;本文件不重复列变更清单,只给出修订后的完整、自洽的规范。
>
> 阅读顺序:先读第 3 节的符号-模块映射表建立全局图景,再按需要跳到具体章节。第 4 节的代码库结构是所有后续章节的落点——每个数学对象、每条处理规则都应该能在这张目录树里找到唯一归属。

---

## 1. 项目一句话

从**公开且有合法使用权的多细胞系 gene-knockdown Perturb-seq 数据**中学习一个跨细胞系共享、由 perturbation 证据约束的 GRN-ODE;对比赛给出的新细胞系,只用其 NTC 单细胞表达确定初始状态,再模拟指定 knockdown 后的表达分布。赛制已确认为 zero-shot cell-context generalization:目标细胞系不提供任何该细胞系的 KD 监督数据。

这不是直接复用某一个预测器。复用的是成熟的 GRN 候选算法、ODE 参数化和数值求解器;项目需要构建的是"哪些机制跨细胞系共享、哪些差异只来自初始状态"的训练组织和检验方法。

## 2. 官方任务与数据边界

| 阶段 | 可见信息 | 允许用途 | 不可见或不可用的信息 |
| --- | --- | --- | --- |
| 比赛训练/开发阶段 | 官方指定的外部可用数据范围(若官方额外提供物种/组织来源等元信息,以官方说明为准) | 训练共享机制、内部验证、模型选择 | 与比赛验证/测试细胞系重合的隐藏标签不得使用 |
| 外部训练域 | H1 与其他有合法权限的公开/私有 perturbation 数据 | 训练共享机制、内部验证、模型选择 | 与比赛验证/测试细胞系重合的隐藏标签不得使用 |
| 比赛验证阶段 | 验证细胞系 NTC 与待预测的 KD 清单(zero-shot,已确认无该细胞系 KD 监督数据) | 生成提交预测;若有榜单,仅作黑箱评测信号 | 响应标签视为隐藏,不得用于训练/调参 |
| 比赛测试阶段 | 留出测试细胞系 NTC,以及官方指定的待预测扰动 | 最终推理与提交 | 测试细胞系扰动后表达不得用于训练、调参、候选图选择或融合权重选择 |

比赛说明明确允许使用任何具有适当权限的数据。**收集和审计外部多细胞系 perturbation 数据是本项目的第一步**,而不是附加项。模型的真实开发验证必须在外部训练域内部完成;比赛验证集不是带可见 GT 的常规 validation set。

> **合规风险前置提醒**:P0 清单中的 Arc VCC 2025 数据集本身就是 H1 hESC 的官方扰动数据。若 2026 目标细胞系仍是 H1 或与其标签重合,该数据集不得进入训练域。详见 5.0 节。

## 3. 符号 - 模块映射表

实现时,每个数学符号应严格对应下表中的模块,禁止跨模块隐式复用同名变量导致语义漂移。

| 符号 | 含义 | 归属模块(见第4节目录树) | 生命周期 |
| --- | --- | --- | --- |
| `X_e` | 环境 e 下的单细胞原始表达矩阵 | `data/registry`, `data/ingestion` | 只读,来自外部数据 |
| `S_e` | 环境 e 的预干预状态(由 matched NTC 估计) | `causal/matching.py` | 每次因果证据生成时计算 |
| `H_pre` | 环境 e 的预处理/设计协变量(平台/协议/批次等) | `causal/matching.py`, `data/registry` | 元数据,来自登记卡 |
| `d_{e,a}` | 环境 e 中 KD `a` 的 E1 assignment effect(program 空间) | `causal/matching.py` -> `causal/evidence_store.py` | 因果前端输出 |
| `tau_a(S)` | KD `a` 的 E3 transport effect 作为 S 的函数 | `causal/hierarchical_effect.py`, `causal/weighting.py` | 因果前端输出 |
| `E2` | 实际敲低强度对下游响应的 dose effect | `causal/guide_iv.py` | 仅条件触发 |
| `E4` | 给定目标 NTC 与 target 的比赛预测分布 | `inference/submit.py` | 交付/提交对象 |
| `W` | Program 空间投影矩阵 | `representation/program_space.py` | 每个外层训练折内单独拟合 |
| `z(0)` | 初始状态(program 空间) | `representation/program_space.py` | 每个细胞系/环境各自计算 |
| `G_predictive, theta (A,B,b,Lambda)` | 共享预测耦合与动力学参数 | `grn/mechanism_graph.py`, `model/ode.py` | 训练期学习,跨细胞系共享 |
| `M` | 候选图(候选边+证据字段) | `grn/candidates.py` | P0-P2 阶段构建 |
| `u_a` | KD 的 soft intervention | `model/ode.py` | 推理时按目标基因构造 |
| `R_phi, h_c` | 低秩 context adapter 及其 NTC 摘要输入 | `model/adapter.py` | 默认关闭,P5 门控启用 |
| `L_bridge` 及 `lambda_bridge` | 桥接损失及其权重(Anchor Regression 校准) | `training/losses.py` | 训练期 |

## 4. 代码库结构建议

```text
vcell/
├── data/
│   ├── registry/            # 每个 dataset 的信息登记卡(见 5.1),YAML/JSON 存储
│   ├── ingestion/           # 按数据源拆分的下载/解析脚本,每个数据源一个子模块
│   ├── qc/                  # 表达矩阵QC、gene panel对齐、深度归一化、协议/平台标准化
│   └── summary_report.py    # 生成 5.2 汇总报告(含谱系/功能覆盖度诊断)
├── causal/
│   ├── matching.py          # E1 matched NTC assignment effect,环境级 S_e/H_pre 计算
│   ├── strata.py            # S 分层与公共支持域裁剪(6.2)
│   ├── hierarchical_effect.py # 默认 condition-level 分层效应/meta-regression
│   ├── weighting.py         # E3 条件触发的稳定平衡权重(6.4)
│   ├── doubly_robust.py     # E3 条件触发的 AIPW/DR 摘要(6.4)
│   ├── guide_iv.py          # E2/rho 校准的可选 guide-IV 支路
│   ├── invariance.py        # 可选 ICP/JCI/跨环境等效性分数
│   ├── backshift.py         # 可选 program 图筛选分数(强假设)
│   ├── proximal.py          # 可选双代理 proximal 支路,默认关闭
│   ├── sensitivity.py       # 可选、按效应尺度注册的敏感性报告(6.7)
│   ├── overlap_audit.py     # P0 前置 go/no-go 审计(5.3)
│   └── evidence_store.py    # 因果证据记录的读写(6.8 schema)
├── representation/
│   └── program_space.py     # PCA/NMF/cNMF 拟合,谱系配额/联合分解(6.9)
├── grn/
│   ├── candidates.py        # GENIE3/GRNBoost2/SCENIC/Inferelator/CellOracle 封装
│   ├── mechanism_graph.py   # G_predictive 的联合约束求解(第8节)
│   └── explain_graph.py     # 训练后外部证据标注,不反向覆盖 G_predictive
├── model/
│   ├── ode.py                # f_{theta,G_predictive},数值求解器封装,u_a 构造
│   ├── adapter.py            # R_phi,默认关闭
│   └── baselines.py          # no-change / mean-delta / ridge-reduced-rank / one-step
├── training/
│   ├── losses.py              # 总损失(第9节),含受限 anchor-regression 风格的 bridge 项
│   ├── folds.py                # 外层训练折构造,防止 W/G_predictive/theta 跨折泄漏
│   └── trainer.py
├── validation/
│   ├── loco.py                  # leave-one-cell-line-out
│   ├── lopo.py                  # leave-one-perturbation-out
│   ├── double_holdout.py        # cell-line + perturbation 双留出
│   ├── state_swap.py            # context permutation
│   ├── overlap_balance_audit.py # 训练环境可比性复核(与5.3的P0审计区分:这是P2之后的复核)
│   ├── placebo.py                # matched-NTC / replicate placebo
│   ├── shared_vs_free.py         # 共享ODE vs 自由per-cell-line模型
│   ├── graph_ablation.py         # 无图 vs 真图 vs 随机图
│   └── ode_vs_onestep.py         # 连续结构增益检验
├── eval/
│   ├── metrics.py               # 官方 Cell-Eval 封装
│   └── calibration.py            # 提交前指标感知校准(11节)
├── inference/
│   └── submit.py                 # 目标细胞系 NTC -> 提交格式
├── configs/                       # 每个训练折/实验的超参数快照
└── tests/
    ├── unit/                      # 每个 causal/* 函数的性质测试(见12节)
    └── integration/                # 合成数据端到端测试
```

## 5. 数据策略与 P0 获取

### 5.0 P0 数据集清单(基于用户提供的人工审核表,共 15 个来源)

精简视图(完整字段结构见 5.1 schema;`下载状态`/`占用空间`两列直接采用用户原表的字段名,作为登记卡的工程追踪字段):

| dataset_id | 细胞系/谱系 | 扰动模态 | Gene panel 规模 | 细胞数(约) | quality_tier(建议) | training_role |
| --- | --- | --- | --- | --- | --- | --- |
| Arc VCC 2025 | H1 hESC(多能干细胞) | CRISPRi KD | 300 基因 | 30 万 | B(见下方合规提醒) | 待定,取决于2026目标细胞系身份 |
| Balmas iPSC atlas | 34 条 iPSC 系,26 供体 | CRISPRi KD | 全基因组7,226 + 重复panel444(19系) | 85.4 万 | **A** | 主训练域,首选overlap anchor |
| Replogle 2022 | K562(白血病)、RPE1(视网膜上皮) | CRISPRi KD | K562全基因组9,866/essential2,057;RPE1 essential2,393 | 254.8 万 | **A** | 主训练域,首选overlap anchor |
| Mixscale | A549/MCF7/HT29/HAP1/BxPC3/K562(6系,跨肺/乳腺/结直肠/髓系/胰腺/白血病) | CRISPRi KD | 5通路panel,各44–61基因 | 约260万 | **A** | 主训练域,首选overlap anchor(注意剥离细胞因子刺激条件) |
| X-Atlas Orion | HCT116(结直肠)、HEK293T(肾源转化) | CRISPRi KD | 全蛋白编码基因组 | 800万+ | **A** | 主训练域,overlap anchor(注意约521GB存储开销) |
| Southard TFome | Hs27(原代成纤维)、RPE-1 | **CRISPRa** | 1,482个TF | 268万 | B | 第二证据梯队,`eval_only`,不进主KD训练域 |
| Compressed Perturb-seq | THP-1(AML) | **混合**:Cas9 KO + CRISPRi KD | 598免疫应答基因 | 21万 | B(仅CRISPRi子集) | 需拆分:KO子集排除出主域,CRISPRi子集按单系B级使用 |
| sci-Plex-GxE GBM | A172/T98G/U87MG(3系胶质母细胞瘤) | CRISPRi KD + 4种药物剂量 | 522激酶 | 105万 | **A**(仅DMSO子集) | 需取DMSO-only子集,药物条件不入主KD域 |
| TRADE HepG2 | HepG2(肝癌) | CRISPRi KD | ~2,393 DepMap common-essential | 13.4万 | B(单系,但essential panel很可能与Replogle高度重叠) | 主训练域,essential骨干候选 |
| PerturbFate melanoma | A375(黑色素瘤) | CRISPRi KD + vemurafenib | 143基因 | 30万+ | B(仅DMSO子集) | 需取DMSO-only子集 |
| EndoC-βH1 | EndoC-βH1(胰岛β细胞,内分泌) | CRISPRi KD | 61+40(T2D+RQC) | 未汇总,需自行统计 | B | 谱系多样性补充,不指望overlap |
| TeloHAEC | TeloHAEC(主动脉内皮) | CRISPRi KD | 2,885启动子靶基因 | 21.4万 | B | 谱系多样性补充,长尾节点覆盖 |
| HepaRG | 分化HepaRG(非癌肝细胞样) | CRISPRi KD | 11基因 | 未汇总,需自行统计 | C(panel极小) | 与HepG2配对,用于"同谱系/不同转化状态"验证场景 |
| Tian iPSC-neuron | WTC11 iPSC 及其诱导神经元(同一遗传背景两种分化态) | CRISPRi KD | 92基因(生存相关) | 45.8万 | B | 谱系多样性 + 状态/机制解耦真实验证场景(见下) |
| Dual-model microglia | iTF-MG / iMG(两种诱导协议,同一目标细胞类型) | CRISPRi KD | 31基因 | 未汇总,需自行统计 | B/C | 协议(H) vs 真实状态(S)分离的真实验证场景(见下) |

**结构概览**:4 个 A 级骨干数据集(Balmas、Replogle、Mixscale、X-Atlas Orion)贡献约 1,400 万+细胞、覆盖约 44 条细胞系/iPSC 系,是跨细胞系 overlap 证据的主力;其余 9–10 个 B/C 级数据集大多是单细胞系、几十到几千基因的小规模谱系补充,主要贡献"全面覆盖"的谱系多样性而非 overlap 强度;1 个 CRISPRa 数据集按第二证据梯队处理;3 个数据集(Compressed Perturb-seq、sci-Plex-GxE GBM、PerturbFate melanoma)在进入主训练域前需要先做 modality/条件子集拆分。



### 5.1 数据登记卡 schema

**每个 dataset 一份**,字段如下(相对 v1 新增了谱系/组织来源与依赖性分层两个字段,用于支撑"均匀且全面覆盖"的量化审计,而不只是质量分级;`download_status`/`storage_size` 对应用户原表的"下载状态"/"占用空间"列):

```yaml
dataset_id: string                    # 唯一标识
source: string                        # 文献/数据库来源、DOI 或 accession
license_and_permission: string        # 使用权限说明,必须明确可训练/可发表使用
species: enum[human]                  # 首版只收人源
cell_model:
  cell_line_or_type: string
  lineage_ontology: string             # 谱系/组织来源标注(Cellosaurus/CL本体或等价)
  depmap_dependency_tier: enum[core_essential, contextual_essential, non_essential, unknown]
perturbation:
  modality: enum[CRISPRi_KD, CRISPRa, other]   # 首版主训练域仅用 CRISPRi_KD
  guide_library: string
  targets: list[gene_symbol]
  efficiency_measured: bool            # 是否有实测敲低效率,供 5.3 节 rho 估计使用
assay:
  protocol: string                     # 10x Flex / 其他
  sequencing_platform: string
  read_depth_summary: object
timepoint: string                      # 相对干预的时间窗
batch_replicate_structure: object      # study/cell_line/batch/replicate/time 的完整交叉表
ntc_matching:
  has_matched_ntc: bool
  matched_ntc_granularity: enum[per_environment, per_cell_line_only, none]  # per_cell_line_only 需降级处理
quality_tier: enum[A, B, C]            # 对应 v1 4.1 节定义
training_role: enum[main_domain, eval_only, needs_subsetting, pending_compliance_check]
tracking:
  download_status: enum[not_started, in_progress, done]
  storage_size: string                 # 如 "521GB",用于P0存储/算力预算
notes: string
```

`quality_tier` 判定规则沿用 v1:

| 等级 | 数据特征 | 用途 |
| --- | --- | --- |
| A | 多细胞系、相同或可比 KD、matched NTC、时间/协议明确 | 共享 GRN-ODE 的主要证据 |
| B | 单细胞系或 KD 覆盖有限,但质量和元数据可靠 | 扩展节点/干预覆盖;较低权重 |
| C | 协议、扰动类型或时间窗显著不同 | 仅用于候选边、预训练表示或独立敏感性分析 |

CRISPRa 数据登记后标记为第二证据梯队,`quality_tier` 之外额外打 `training_role: eval_only`,不进入 6 节因果前端的主训练证据流,只在模型评估和后续加强训练阶段使用。

### 5.2 汇总报告 schema

`data/summary_report.py` 应对整个 P0 语料输出结构化报告,不是纯文字总结:

```yaml
total_datasets: int
by_quality_tier: {A: int, B: int, C: int}
lineage_coverage:
  distinct_lineages: int
  coverage_score: float        # 建议用谱系分层后的有效样本量或 Simpson 多样性指数
  underrepresented_lineages: list[string]   # 覆盖度审计的直接产出,供数据补充决策使用
functional_coverage:
  by_dependency_tier: {core_essential: int, contextual_essential: int, non_essential: int}
  pathway_module_gaps: list[string]   # 主要调控模块(TF枢纽/信号/代谢/结构基因)中覆盖薄弱的部分
overlap_summary: <见 5.3>
```

**实例化结果(基于 5.0 清单,元数据层面的估计,精确数字待 5.1 登记卡逐条填完后由脚本产出)**:

```yaml
total_datasets: 15
by_quality_tier: {A: 4, B: 9, C: 2}     # 估计值,含需拆分后降级的条目
lineage_coverage:
  distinct_lineages: ~14                 # 多能干细胞/神经元/血液髓系/肺/乳腺/结直肠/肾/胰腺外分泌/
                                          # 胰岛内分泌/肝(癌+非癌)/血管内皮/胶质母细胞瘤/成纤维/小胶质细胞
  coverage_score: 待脚本计算(建议Simpson多样性指数)
  underrepresented_lineages: [心肌, 肾小管上皮之外的肾脏细胞类型, 皮肤角质形成细胞, 肌肉]
    # 当前清单未覆盖,若后续要进一步"全面覆盖"可作为补充数据搜索方向
functional_coverage:
  by_dependency_tier: 待脚本计算,依赖每份数据集实际基因panel与DepMap依赖性数据的比对
  pathway_module_gaps: 多数B/C级数据集是疾病特异性小panel(T2D/MASLD/CAD等),
    覆盖的是"临床相关基因"而非系统性通路采样,不能替代GENIE3/GRNBoost2候选边生成
    所需的广谱调控网络覆盖
```

**核心判断**:谱系广度好于此前"数据可能集中在K562/RPE1/A375/Jurkat几个主流系"的担忧,清单已覆盖约14种谱系。但**规模严重不均**——4个A级数据集贡献约1400万+细胞、约44条系,其余9-10个B/C级数据集大多是个位数细胞系、几十到几千基因的小规模补充。若直接对pooled矩阵做PCA/NMF,谱系代表性会被A级骨干中的癌系/转化系压制。**6.9节的谱系配额策略在这份清单上不是可选项,而是必须执行的一步**,否则K维program空间会带上明显的癌系/转化系偏见。

### 5.3 Overlap 可行性审计(P0 前置 go/no-go)

**在投入因果前端和 GRN-ODE 工程量之前**,先跑这个审计,判断 4.3/6 节的整套 strata + 加权矫正是否有真实的 overlap 支撑。以下是基于清单元数据的**结构性判断**(design 层面能看出多系重叠的可能性),不等于精确的基因级比对——精确重叠仍需拿到各数据集原始基因 ID 列表后用脚本核实,已列入第16节待办任务。

**strong 候选(design 上本身就是多系重叠研究,建议作为 causal front-end 开发和单元测试的首选真实数据)**:
- **Balmas iPSC atlas**:19 条系共享 444 基因靶向 panel,单一 study/协议——是全清单里最干净的 overlap 证据来源(H 的技术协变量变异最小,S 的遗传背景变异真实存在),优先级最高。
- **Mixscale**:6 条相距很远的细胞系(肺/乳腺/结直肠/近单倍体髓系/胰腺/白血病)共享 5 个通路 panel(各 44–61 基因),专为跨系比较设计——第二优先级 anchor,注意先剥离细胞因子刺激条件(IFNβ/IFNγ/TGFβ/TNFα/insulin)。
- **sci-Plex-GxE GBM**:3 条胶质母细胞瘤系(DMSO-only 子集内)共享 522 基因激酶 panel——系间遗传背景差异较小,但仍是可用的 3 环境 overlap,且满足 6.5 节 BackShift 所需的最低环境数。

**中等候选(design 上有重叠可能,需要基因级比对确认)**:
- Replogle(K562 essential 2,057 / genome-wide)+ TRADE HepG2(DepMap common-essential ~2,393)——两份 essential 基因集合规模接近,很可能高度重叠,若确认将横跨白血病/肝癌两个远缘谱系,是全清单最值得优先核实的一组,建议列为 P0 第一个脚本任务。
- Replogle K562 与 Mixscale K562(同一细胞系、不同 study/协议、不同基因 panel)——这一对不贡献新的跨系证据,但是检验"协议差异是否只是可被消去的 nuisance H"这一假设的天然场景,建议同时用于 6.1 差分方法的实证检验(而不只是合成数据单元测试)。

**weak/暂不指望 overlap 矫正的部分**:
EndoC-βH1(61+40基因)、HepaRG(11基因)、TeloHAEC(2,885基因但单系)、Tian iPSC-neuron(92基因)、Dual-model microglia(31基因)、PerturbFate melanoma(143基因)——这些数据集的panel多为单系或规模很小,难以在4.3/6节贡献真正的跨系overlap证据。它们的价值在于**谱系多样性**和下方两个**特定真实验证场景**,不必强求纳入overlap矫正的主证据流,按B/C级 + 侧重长尾节点覆盖的方式使用即可。

**清单自带的两个高价值真实验证场景(建议纳入第10节验证矩阵,作为合成测试之外的真实fixture)**:
1. **Tian iPSC-neuron(WTC11)**:同一遗传背景在两种分化状态(iPSC/神经元)下各自的 NTC + KD 数据,是"state swap / context permutation"验证的真实数据实例,比纯合成置换测试更有说服力。
2. **Dual-model microglia(iTF-MG vs iMG)**:同一目标细胞类型、两种不同诱导协议产生的数据,是检验"协议差异是否真的只是nuisance H、而非真实S差异"的天然实验场景,建议作为matched-NTC/replicate placebo验证的真实fixture之一。

精确核实上述结构性判断的具体算法(`causal/overlap_audit.py` 实现):

```text
对每个候选 KD 基因 a:
  1. 统计有 matched NTC 的环境集合 E_a = {e : a 在 e 中被敲低且有 matched NTC}
  2. n_cell_lines(a) = E_a 中不同细胞系的数量
  3. 计算 E_a 中各环境 S_e 的两两重叠(如 S 空间中的核密度重叠或凸包交集比例)
  4. 输出: overlap_tier(a) ∈ {strong (n_cell_lines>=3 且 S 支持域重叠充分),
                              weak (n_cell_lines in [2, 3) 或支持域重叠有限),
                              none (n_cell_lines<2)}

汇总:
  - strong 覆盖比例 = |{a: overlap_tier(a)=strong}| / |全部候选 KD|
  - 若 strong 覆盖比例过低(建议起始阈值 <30% 时触发复核,该阈值需按实际数据规模重新校准,
    不作为固定标准),需要在正式投入 6 节因果矫正管道前,评估是否放宽 overlap 定义
    (如放宽到"同源基因/同通路基因"层面借用 overlap),并在方法卡中显式记录放宽规则。
```

审计产出直接写入 `overlap_summary`(见 5.2),作为项目决策的依据而不是事后诸葛亮式的验证项。

## 6. 训练前处理:因果证据与运输管道

本节是 v1 第 4.3 节的展开实现规范。先冻结实验内效应 DAG、跨环境运输 DAG 和四层 estimand，再按 `6.1 E1 → 6.2 overlap/支持 → 6.3 默认层级汇总 → 6.4 条件平衡/DR → 6.5 可选不变性/BackShift → 6.6 条件 E2 guide-IV/proximal → 6.7 敏感性 → 6.8 落盘` 执行。任何方法都不能把 E1、E2、E3 和 E4 合成一个“已校正结果”。

### 6.0 双 DAG 与四层 estimand

实验内 DAG 区分 target/guide assignment `Z`、实际敲低强度 `K`、预处理状态 `S`、终点表达 `Y`、扰动后指标/中介 `Q_post` 和存活/捕获选择 `V`；跨环境 DAG 区分 NTC state `S`、预处理/设计变量 `H_pre`、KD coverage、未记录偏差 `U` 和目标状态分布。S02 必须登记 assignment unit、目标总体、效应尺度、positivity、selection/interference 及禁止解释。

| estimand | 定义 | 用途 |
| --- | --- | --- |
| `E1_assignment_itt` | 同环境 target/guide assignment 相对 NTC 的终点分布效应 | 默认 evidence |
| `E2_knockdown_dose` | 实际靶基因敲低强度对下游响应的效应 | 可选 guide-IV/`rho` 校准 |
| `E3_transport` | 将外部 E1/E2 运输到预定义目标 NTC 状态分布的效应 | 共享证据与 OOD 解释 |
| `E4_prediction_distribution` | 给定目标 NTC 与 target 产生的 raw-count 预测分布 | 比赛交付 |

E2/E3 不可识别不阻止获批的 E4 fallback；但预测输出不得反向升级任何因果 estimand。

### 6.1 Matched NTC assignment effect（不是经典 DID）

对每个实验环境 `e=(study, cell_line, batch, replicate, time)`:

```text
S_e = summarize(matched_NTC_cells(e))              # 预干预状态摘要(program空间均值/分布)
d_{e,a} = E[psi(X_{e,a})] - E[psi(X_{e,0})]         # psi: program空间映射,X_{e,0}即该环境NTC
```

**识别假设(必须在代码注释和方法卡中显式声明)**:若 target/guide assignment 在环境内近似随机且 NTC 与 KD 的环境设计可比，此步骤估计 E1 assignment effect；它只消去对 KD 和 NTC **都相同的加性基线/批次项**。它不是经典 DID，不需要凭终点横截面数据宣称 parallel-trends。若存在 `KD x batch` 交互、选择或干扰，此步骤不自动消除偏差。

**实现约束**:
- 独立单位是"有 matched NTC 的 KD 条件/重复",不是单个细胞;单细胞数量只降低该条件响应分布/方差的估计误差,不产生额外 overlap。
- 原始单细胞分布必须保留,不能把 `d` 当作伪配对单细胞轨迹使用。

### 6.2 Strata 划分与公共支持域裁剪

```text
对同一 KD a:
  按 S_e 的相似度(如 program 空间聚类或核密度)划出 strata
  只在 S 的共同支持域内比较不同环境(丢弃支持域外的环境,而非外推)
  每个 strata 记录:环境数量、S 中心、支持域边界
```

### 6.3 默认层级效应汇总

对每个 target/program/候选边先做 condition/replicate 级低维 hierarchical meta-regression：

```text
d_{e,a,g} = mu_{a,g} + beta_{a,g}^T S_e
            + b_study + b_protocol + b_batch + epsilon_{e,a,g}
```

`mu` 表示共享成分，`beta` 表示 NTC state effect modification，技术项只在存在交叉重复时估计。报告异质性、prediction interval、leave-one-environment-out、sign consistency 和单环境 influence。环境数少时限制 moderator/随机项数量，不能将单细胞数量当作更多独立环境。

### 6.4 条件 Overlap Weighting

只有独立环境数、交叉重复和 positivity 足够时，在每个 strata 内用广义倾向得分平衡允许的预处理/设计协变量 `H_pre`；`QC` 不再作为默认可调整变量:

```text
对环境 e,估计 e(H_pre) = P(环境=e' 的类别 | H_pre)     # 多类广义倾向得分(如多项logistic)
overlap_weight(e) ∝ 1 / e(H_pre)_调和平均              # Li & Li (2019) 多处理组推广形式
```

Overlap weighting、entropy balancing 或 kernel balancing 只作为条件分支。必须与未加权/逆方差 hierarchical meta-regression 比较 balance、ESS、单环境 influence、prediction interval 和 leave-one-environment-out 稳定性；若无改善或 ESS 崩溃，保留层级基线并标记 `limited_transport`。

### 6.4.1 双重稳健(AIPW)

```text
tau_hat_a(S) = weighted_mean(d_{e,a} * overlap_weight(e))            # 加权部分
             + outcome_regression_correction(S, H_pre)                # 结局回归修正
```

`outcome_regression_correction` 用一个轻量回归模型拟合 `d_{e,a}` 关于 `(S,H_pre)` 的关系；只有在独立环境足够、可交叉拟合且 positivity 可诊断时运行。不能把“一个 nuisance 模型正确”写成无条件保证；结果仍依赖未观测偏差、选择和 interference 边界。

### 6.5 ICP/JCI/BackShift 可选候选筛选通道

ICP/JCI 只能作为跨环境不变性候选分数；不拒绝异质性不能证明不变。BackShift 的适用条件为 `G_predictive` 可有反馈环、至少三个充分不同且 metadata 可辨环境、线性化、shift-intervention、equilibrium 和隐藏混杂稳定等假设有诊断依据。

```text
输入: 各环境(视为不同 shift-intervention 设定)的 program 空间协方差矩阵 {Cov_e}
要求: 至少 3 个充分不同的环境设定；NTC 不自动等于合格 shift intervention
方法: 对协方差矩阵差异做联合矩阵对角化,估计带潜变量的线性循环因果图
输出: 每条候选边的 backshift_identifiability_score,写入第8节候选边字段
```

该通道只作为候选筛选分数，不直接产出最终 `G_predictive`，不证明直接调控，也不进入 MVP 关键路径。

> 候选数据:5.3 节识别出的 Balmas iPSC atlas(19 环境)、Mixscale(6 环境)、sci-Plex-GxE GBM DMSO 子集(3 环境)满足“环境数量不少于3”这一最低结构条件,但仍必须现场检查 shift、equilibrium、节点一致性和隐藏混杂稳定性；其覆盖的基因(444 / 5×44–61 / 522 个)只能作为 BackShift 通道的候选验证对象。

### 6.4.2 数据配额与 weight interaction 审计

从`5.2`可以看出各数据集规模差异悬殊(A级4个数据集贡献约1400万+细胞、约44条系,B/C级数据集普遍是几万到几十万细胞、个位数细胞系)。若无限制地直接混合训练,模型极易被数据量占优势的细胞系或研究主导,削弱跨谱系的泛化能力。建议:

- 训练阶段对不同数据集设置**采样权重上限**,避免任一数据集(尤其是 X-Atlas Orion 的800万+细胞)在梯度更新中占据压倒性比例;
- 数据集/谱系采样权重只描述 representation/training sampling，E3 balance weight 描述目标总体/可比性；两者必须分开存储，并在组合前后检查 ESS、单环境 influence 和极端值;
- 具体权重方案(如按数据集做 sqrt(n) 缩放、或按谱系做 stratified batch sampling)留待数据实际到位、6.9 节谱系配额策略和 S02 T00 现场调研后确定；不得把任一权重写成自动识别未观测混杂的工具。

### 6.6 E2 guide-IV 与条件 Proximal / 负对照支路

**guide-IV 触发条件**: target 有多条近似独立 guide、`K` 可可靠测量、first stage 足够强、排除限制/单调性/脱靶/载体应激和存活选择可审计。只估计 E2 或校准 `rho`，不替代 E1。

**proximal 触发条件**: 只有存在独立设计支持的负对照暴露和负对照结局，并且可论证相关性与 bridge 完备性时才评估；否则 `not_identifiable`。

```text
1. 从独立实验设计或外部证据定义负对照暴露/结局；候选图 M、待分析响应和“管家基因”本身不能循环保证负对照成立
2. 审计其时间顺序、相关性、完备性和是否受 target/选择/应激影响
3. 按 proximal causal inference 的识别条件(NC/负对照暴露的相关性+完备性条件)
   构造对 tau_a(S) 的非参数矫正项
4. 若 NC 数量不足或不满足相关性/完备性条件,记录为"无法识别",
   不得强行输出矫正结果——转 6.7 做敏感性分析而非声称已矫正
```

方法依据:proximal causal inference(Miao, Geng & Tchetgen Tchetgen, Biometrika 2018 及后续工作)。该支路不是默认的非加性混杂修复器；没有有效双代理时不能输出矫正值。RUV 或技术控制也不能自动满足 proximal bridge 条件。

### 6.7 按效应尺度注册的敏感性分析

当前 program/gene continuous effect 不默认计算 E-value。E-value 仅在处理、结局和效应尺度满足其方法定义时注册；回归型连续效应可评估 robustness value/部分 R² 敏感性，但二者都不能替代 overlap、selection、interference 或 transportability 诊断。没有匹配尺度的方法时保留定性 sensitivity label 和禁止结论。

### 6.8 证据记录 schema

`causal/evidence_store.py` 落盘的每条记录:

```yaml
estimand_id: enum[E1_assignment_itt, E2_knockdown_dose, E3_transport, E4_prediction_distribution]
target_population: string
assignment_unit: string
effect_scale: string
kd_gene: string
strata_id: string
d: array            # program 空间条件效应
S: array            # 预干预状态摘要
H_pre: object        # 允许的预处理/设计协变量
covariate_role_manifest_ref: string
allowed_adjustment_set: array
weight: float        # 6.4 输出
variance: float       # 估计方差
support_flag: enum[in_support, trimmed_out]
doubly_robust_applied: bool
guide_iv: {attempted: bool, identifiable: bool, first_stage_status: string, rho_calibration_status: string}
proximal_correction: {applied: bool, proxy_design_ref: string, identifiable: bool}
sensitivity: {method: string, scale: string, status: string, value: float}
heterogeneity_ref: string
prediction_interval_ref: string
selection_interference_flags: object
invariance_score: float
backshift_score: float   # 仅在满足前置条件时
```

**禁止**:任何阶段都不得输出"一份被覆盖写掉的无混杂表达矩阵"替代这份逐条记录——下游训练必须能追溯每条证据的矫正过程。

### 6.9 Gene program 基组的谱系配额拟合

直接对 pooled 矩阵做 PCA/NMF 会被样本量最大的细胞系(通常是癌系)主导,导致 `W` 带谱系偏见,破坏"状态/机制"解耦的前提。`representation/program_space.py` 实现两种可切换策略:

```text
策略A(联合分解): 每个细胞系/谱系单独因子化后,求跨谱系一致的共享因子(类似 MOFA+ 思路),
                 要求 K 个 program 必须在所有谱系里都可重建,而非只在优势谱系里解释力强

策略B(配额重采样): 拟合 W 时按谱系加权重采样细胞,使各谱系贡献大致相当
                    (这是 representation sampling，不是 6.4 的 E3 overlap weighting)
```

`W` 在每个外层训练折内单独拟合,避免留出细胞系泄漏(v1 已有要求,此处保留)。除重构和 seed 稳定性外，必须报告 `S_e` 与 `d_{e,a}` 的轴重叠、dataset/context/target 可预测性和 NTC-only/all-cell basis 差异；这些是纠缠诊断，不自动证明状态与响应已被识别性分离。

## 7. 主模型规范

```text
z_{c,i}(0) = E(X_{c,i}^{NTC})                                  # representation/program_space.py
dz_{c,i}(t)/dt = f_{theta,G_predictive}(z_{c,i}(t), u_a)        # model/ode.py
X_hat^a_c = D(z_{c,i}(T))                                        # model/ode.py (解码)
```

```text
z = W^T x_tilde,  x_hat = D(z)                                   # representation/program_space.py
dz/dt = A sigma(Bz + b + u_a) - Lambda z,  G_predictive = AB    # model/ode.py
u_a = -rho_{c,a} q_a                                              # model/ode.py
f_c(z,u) = f_{theta,G_predictive}(z,u) + R_phi(h_c; z,u), h_c=Pool(E(X_c^NTC))  # model/adapter.py, 默认关闭
```

约束(v1 已定义,此处作为实现契约重申):`A,B,b,Lambda` 跨训练细胞系共享;`G_predictive` 允许反馈环、不强行裁成 DAG;稀疏、低秩、数值稳定约束避免用参数容量记忆 `cell line x KD`;目标细胞系身份不作为可记忆 embedding;单终点数据仅支持有限时间生成解释,不宣称恢复真实中间轨迹。`rho_{c,a}`:若测得敲低效率,直接取自 5.1 登记卡中 `efficiency_measured` 对应值;若 S02 E2 guide-IV 通过识别闸门，可使用其校准；否则作为 bounded/regularized fold-learned variable，并显式记录来源和不确定性。`R_phi` 只在 leave-one-cell-line-out(11节)稳定提升时启用,`lambda_adapter` 默认给出强正则。

## 8. GRN 构建规范

四类对象(候选图 `M`、干预效应证据、预测耦合 `G_predictive`、解释图),`grn/candidates.py` 产出的每条候选边字段:

```yaml
edge: [source_gene, target_gene]
shared_score: float          # 跨细胞系一致性
evidence_type: string        # total effect / state conditional / predictive coupling
total_effect_support: float
state_conditional_effect: float
sign_consistency: float
effect_size: float
context_interaction: float   # 是否存在细胞系特异性交互
protocol_stability: float
heterogeneity_ref: string
prediction_interval_ref: string
equivalence_status: string
temporal_support: string
prior_support: float         # 知识图谱先验强度
direct_regulatory_edge_status: string    # 默认 unresolved
invariance_score: float                   # 可选 ICP/JCI
backshift_identifiability_score: float   # 可选且可为空
```

`grn/mechanism_graph.py` 中最终 `G_predictive` 由联合约束求解:扰动后分布拟合、被敲低基因方向一致性、跨细胞系共享风险、稀疏性、稳定性；干预效应证据只作为带支持域和不确定度的输入，`backshift_identifiability_score`/`invariance_score` 只作为可选辅助项，不单独决定边的取舍。`direct_regulatory_edge_status` 默认保持 `unresolved`。必须比较无先验、真实先验、degree-matched 随机图三组(见 10 节图消融)。

## 9. 训练目标、复杂度门控与基线阶梯

```text
L = L_distribution^weighted + lambda_KD*L_KD + lambda_bridge*L_bridge
  + lambda_sparse*||G_predictive||_1 + lambda_prior*L_soft_prior + lambda_adapter*||R_phi||^2
```

`L_bridge` 只作用于完成 6.2 共同支持裁剪与证据审计的桥接证据,检验共享 ODE 是否解释这些环境间的条件效应,不惩罚由 `S` 解释的真实响应异质性。它不等同于 S02 的 evidence weight：前者是模型鲁棒性约束，后者是证据目标总体/可信度。

**`lambda_bridge` 取值(受限 Anchor Regression 化)**:默认只把有明确技术语义的 study/protocol/batch 指示变量作为 anchor；cell line/lineage 含真实 effect modification，不能默认视为 nuisance。`lambda_bridge` 对应鲁棒性压力参数，但其取值只能在 inner folds 搜索，不能用 outer LOCO 或 S07 adapter 结果回流重调。必须比较 `weight_off/bridge_off/both_on`，并检查 state signal、loss scale、ESS 和单环境 influence。

复杂度升级阶梯(v1 保留,门槛建议量化如下,均为**起始草案**,需按内部验证实测结果调整):

| 阶段 | 模型 | 升级门槛(建议起始值,需校准) |
| --- | --- | --- |
| P0 | 外部数据获取、许可/元数据/配对审计 | 5.3 overlap 审计通过 |
| P1 | no-change、平均 delta、ridge、B2H/reduced-rank | 建立基线后即视为已完成,不可跳过 |
| P2 | 共享线性 GRN-ODE | 相对 P1 最优基线,LOCO 关键指标有统计显著提升 |
| P3 | 共享 nonlinear GRN-ODE | 相对 P2,LOCO 关键指标有稳定提升(建议:多次 fold 重复,提升非单次偶然) |
| P4 | 软知识先验 | 图消融(10节)显示真实先验优于 degree-matched 随机图 |
| P5 | 低秩 adapter / MoE | P3/P4 的 LOCO 残差分析显示存在系统性、可由 NTC 预测的残差 |

## 10. 内部验证矩阵

9 项验证(v1 定义),补充 biology/protocol 分离与 estimand 边界:

| 验证 | 要回答的问题 | 判据草案(需按实测校准) |
| --- | --- | --- |
| leave-one-cell-line-out | 只用留出细胞系 NTC 时机制能否迁移 | 留出细胞系上的关键指标不显著劣于训练细胞系内交叉验证水平 |
| 同 study 内 LOCO | 协议近似固定时能否跨生物背景泛化 | 仅在连接性和独立 group 足够时运行，否则 `not_estimable` |
| 同 cell line 跨 study | 相同背景能否抵抗协议变化 | 仅在同背景多 study 有重复时运行，否则 `not_estimable` |
| leave-one-study-out / lineage-out | 新协议或更远谱系能否迁移 | 作为原因定位，不替代主 LOCO |
| leave-one-perturbation-out | GRN 能否传播未见 KD | 未见 KD 上的方向一致性(DEG符号)显著优于随机/mean-delta基线 |
| cell-line + perturbation 双留出 | 状态和机制能否同时外推 | 双留出指标劣化幅度应与单独两项劣化之和量级相当,不应出现非线性暴跌 |
| state swap / context permutation | 预测是否依赖正确 NTC 状态 | 错配 NTC 后指标应显著劣化;若不劣化说明模型未真正利用状态 |
| overlap / balance audit(训练后复核) | 各 KD 训练环境是否可比,权重是否稳定 | 权重方差/有效样本量在合理范围,无极端权重主导单一估计 |
| matched-NTC / replicate placebo | E1 是否保留其假设边界,是否仍有交互 | 同一 NTC 内部重复的"伪效应"应接近零;偏离时记录选择/交互原因，不自动进入 proximal |
| shared ODE vs free per-cell-line | 共享假设是否足够 | 自由模型在训练细胞系内可能更优,但在 LOCO 上不应优于共享模型 |
| 无图 vs 真图 vs 随机图 | 图是否有超出容量/度分布的贡献 | 真图显著优于 degree-matched 随机图,而不仅是优于无图 |
| ODE vs 同容量 one-step | 连续结构是否带来实际增益 | 同容量对比下 ODE 版本指标不劣于 one-step,否则移除连续结构假设 |

所有图阈值、基因筛选、表示拟合(`W`)、早停和超参数仅在每个训练折内确定。指标按 cell line、study/protocol、lineage、support/transport class 和 perturbation 分层,至少包括 expression/delta 误差、相关性、DEG 方向、分布距离和方差校准。E4 结果只用于预测比较，不回写 E1-E3 或 direct-edge 状态。

## 11. 评测指标与提交前校准

`eval/metrics.py` 直接接入 Cell-Eval(官方评测工具),覆盖 PDS、DES、MAE,以及 Pearson delta 相关性、log-fold-change 的 Spearman 相关性、AUPRC、效应量 Spearman 相关性。

`eval/calibration.py`:在主模型训练**之外**,单独对已知的打分函数几何性质做后处理校准(如 PDS 的 L1 距离尺度敏感性),在内部 held-out 上拟合校准参数,不进入主模型梯度。这一层与"分布拟合是否物理合理"解耦——后者是模型质量,前者是提交策略。

## 12. 测试策略

给下游 AI agent 的具体测试规范:

**单元测试(`tests/unit/`)**,按性质而非按输出值断言:

```text
test_hierarchical_effect_respects_independent_units:
    构造少量环境与大量细胞的合成数据,验证层级效应汇总不会把细胞数当作独立环境

test_overlap_weighting_exact_balance:
    构造合成数据,仅在满足触发条件时验证权重下允许协变量的加权均值达到预设平衡

test_matched_ntc_diff_removes_additive_batch:
    构造已知加性批次偏移的合成数据,验证 6.1 差分后偏移被消除

test_matched_ntc_diff_fails_under_interaction:
    构造 KD x batch 交互的合成数据,验证 6.1 差分后仍残留可检测偏差(反向测试:
    确认该方法在其声明的失效场景下确实失效,而不是被过度美化)

test_backshift_identifiability_requires_assumptions:
    验证少于3个环境或 shift/equilibrium/稳定混杂假设不满足时 backshift_identifiability_score 输出为空/标记不可用,
    而非静默输出不可信分数

test_proximal_correction_flags_invalid_proxies:
    验证没有独立双代理或相关性/完备性不足时,6.6 输出 identifiable: false,不强行给出矫正结果

test_covariate_role_blocks_post_treatment_adjustment:
    验证 post-treatment mediator/selection/diagnostic 字段不能进入默认 adjustment set

test_e1_e2_e3_e4_are_not_interchangeable:
    验证 evidence、dose calibration、transport summary 与 E4 prediction 的 schema 和 provenance 不可混写

test_program_space_no_fold_leakage:
    验证 W 在训练折与验证折之间没有共享拟合信息
```

**集成测试(`tests/integration/`)**:用已知 effect structure 的合成 Perturb-seq 数据跑完整管道,验证 E1 condition effect、E3 transport uncertainty 与 E4 prediction path 的边界；不以端到端恢复 `G_predictive` 与真实直接结构的接近程度作为因果证明。测试应覆盖线性/反馈、加性+非加性混杂、post-treatment selection、state/protocol 共线、少环境和 guide-IV 失败场景。

## 13. 项目里程碑与保底提交路径

```text
里程碑1(保底基线): P0 overlap 审计 + P1 基线(no-change/mean-delta/ridge/B2H)
                    -> 产出可提交结果,作为整个项目的下限保险
里程碑2: 6节因果前端跑通 + P2 共享线性 GRN-ODE
里程碑3: P3 非线性 GRN-ODE,10节验证矩阵全部跑通
里程碑4(可选): P4 知识先验、P5 adapter,仅在对应门槛(9节)满足时启用
```

里程碑1 应尽早独立于里程碑2-4 完成并可随时提交,避免因果前端或 GRN-ODE 的研发延期导致项目在截止日前没有任何可提交结果。

## 14. 复用边界

| 资产 | 可复用 | 不可替代项目核心 |
| --- | --- | --- |
| PerturbODE | 可解释 ODE、扰动输入和结构化动力学接口 | 必须改造成共享机制/NTC 初态/LOCO 任务结构 |
| MEGA-ODE | 图约束 ODE、GAT 向量场、后续 MoE 思路 | 外部网络先验不能替代从 perturbation 数据学习 `G_predictive`;GPL-3.0 代码不直接复制 |
| 成熟 GRN 工具(GENIE3/GRNBoost2/SCENIC/Inferelator/CellOracle) | 候选边和初始化 | 不直接输出最终 G_predictive 或直接机制图 |
| AROMA / TxPert | 外部预测基线、知识证据、长尾基因参考 | 不作为 ODE 状态、直接因果边或共享预测耦合 |
| PCA/NMF/cNMF、成熟 ODE solver、CellNavi | 状态空间和数值基础设施;CellNavi 仅作图增强表示备选 | 项目不从零实现这些通用组件;CellNavi 不进入首版 |
| hierarchical meta-regression / overlap weighting / AIPW / guide-IV / BackShift / Proximal causal inference | E1-E3 证据汇总或条件触发的辅助分析 | 不替代对识别假设的显式声明与失效场景测试(12节)，不直接定义 E4 或 G_predictive |

## 15. 首版项目边界

```text
主模型:跨细胞系共享、由 perturbation 数据学习的 predictive GRN-ODE
状态:目标细胞系 NTC 决定初始点
机制:共享 G_predictive、theta 决定预测响应规律
干预:KD gene 作为 soft intervention
知识图:候选边和软修正
复用:成熟 GRN 候选工具、PerturbODE/MEGA-ODE 方法组件和 ODE solver
自建:跨细胞系预测耦合训练、E1-E3 证据组织、G_predictive 的数据定权、比赛推理组织、训练前因果证据管道
对照:no-change、mean-delta、ridge、B2H、reduced-rank、同容量 one-step、TxPert/AROMA
```

首版不建设通用数据接口、完整图平台、长期景观分析或自由 per-cell-line GRN。只有 P3 在严格内部 OOD 验证中优于强基线后,才扩展知识图、adapter 或 MoE。

## 16. 待确认/待补充事项清单

**数据集清单(5.0)已提供,以下是由此清单派生出的具体工程任务,建议按顺序作为 P0 的第一批 sprint:**

1. **H1 合规检查(最高优先级)**:确认 2026 目标细胞系是否为 H1,决定 Arc VCC 2025 数据集能否进入训练域(见 5.0 合规风险条目)。
2. **essential 基因交集脚本**:核实 Replogle(K562/RPE1 essential)与 TRADE HepG2(DepMap common-essential)之间的基因符号级重叠规模,这是全清单最值得优先核实的跨系 overlap 骨干。
3. **modality/条件子集拆分脚本**:Compressed Perturb-seq(拆出 CRISPRi-only 子集,排除 Cas9 KO)、sci-Plex-GxE GBM 与 PerturbFate melanoma(各自拆出 DMSO-only 子集),三者拆分完成前不得计入主训练域证据。
4. **未汇总细胞数的自行统计**:EndoC-βH1、HepaRG、Dual-model microglia 三个数据集下载后需自行统计细胞数,填入登记卡。
5. **存储/算力预算**:X-Atlas Orion 单个数据集约 521GB,加上其余数据集,建议在项目启动时明确总存储上限和下载优先级(优先下载 5.3 节标记为 strong/中等候选的数据集)。
6. 6.4 节条件 Overlap Weighting 与 entropy balancing 的具体选择，须在真实倾向分布、独立环境数、交叉重复和 ESS 诊断后决定；不预设 ensemble。
7. 9 节各阶段升级门槛的具体数值,标注为"起始草案",需要在 P1/P2 实测后重新校准为具体阈值。
8. 若官方 2026 数据说明中包含本文件未覆盖的字段或约束(如是否允许组合扰动、是否有额外元数据),需要在数据到位后同步更新第2节任务边界表和第5节登记卡 schema。

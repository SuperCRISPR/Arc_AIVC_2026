# Arc Virtual Cell Challenge 2026 技术路线与项目设计

## 1. 项目一句话

从**公开且有合法使用权的多细胞系 gene-knockdown Perturb-seq 数据**中学习一个跨细胞系共享、由 perturbation 证据约束的 GRN-ODE；对比赛给出的新细胞系，只用其 NTC 单细胞表达确定初始状态，再模拟指定 knockdown 后的表达分布。

这不是直接复用某一个预测器。复用的是成熟的 GRN 候选算法、ODE 参数化和数值求解器；项目需要构建的是“哪些机制跨细胞系共享、哪些差异只来自初始状态”的训练组织和检验方法。

## 2. 官方任务与数据边界

| 阶段 | 可见信息 | 允许用途 | 不可见或不可用的信息 |
| --- | --- | --- | --- |
| 外部训练域 | H1 与其他有合法权限的公开/私有 perturbation 数据 | 训练共享机制、内部验证、模型选择 | 与比赛验证/测试细胞系重合的隐藏标签不得使用 |
| 比赛验证阶段 | 验证细胞系 NTC 与待预测的 KD 清单 | 生成提交预测；若有榜单，仅作黑箱评测信号 | “perturbations to predict”指待预测的扰动身份，不表示提供扰动后 GT；除非官方另行明确发布，否则响应标签视为隐藏 |
| 比赛测试阶段 | 留出测试细胞系 NTC，以及官方指定的待预测扰动 | 最终推理与提交 | 测试细胞系扰动后表达不得用于训练、调参、候选图选择或融合权重选择 |

比赛说明明确允许使用任何具有适当权限的数据。因此，**收集和审计外部多细胞系 perturbation 数据是本项目的第一步**，而不是附加项。模型的真实开发验证必须在外部训练域内部完成；比赛验证集不是带可见 GT 的常规 validation set。

## 3. 一眼看懂

```text
P0  外部公开高质量 KD Perturb-seq 数据
      -> 记录 cell line / perturbation / guide / time / protocol / batch / NTC
      -> matched NTC 差分，按 NTC 初态分层并限制共同支持域
      -> 对每个 KD 生成带权、带不确定性的跨环境因果响应证据

P1  matched NTC 单细胞表达
      -> 稳定 gene-program 状态 z(0)

P2  候选图 + 经校正的跨细胞系 perturbation 证据
      -> 共享预测耦合 G_predictive
      -> 共享 nonlinear ODE f_(theta,G_predictive)

P3  目标细胞系 NTC + 待预测 KD gene
      -> 初始状态 z_target(0) + soft intervention u_a
      -> ODE 积分到实验时间 T
      -> 解码为单细胞基因表达分布并提交
```

唯一主模型为：

\[
z_{c,i}(0)=E(X_{c,i}^{NTC}),\qquad
\frac{dz_{c,i}(t)}{dt}=f_{\theta,G_predictive}(z_{c,i}(t),u_a),\qquad
\hat X^a_c=D(z_{c,i}(T)).
\]

- `z(0)`：目标细胞系的可变初始状态，由该细胞系 NTC 决定；
- `G_predictive, theta`：跨细胞系共享的预测耦合和动力学规律，由外部 perturbation 数据学习；
- `u_a`：指定基因 `a` 的 knockdown 干预；
- `T`：实验终点对应的有限时间窗。

知识图谱只影响候选边和软正则，不能产生或覆盖 `G_predictive`。TxPert、AROMA 等是外部基线或证据层，不是主模型。

状态与机制的拆分假设、以及何时允许引入 adapter，执行时以[机制与状态解耦](参考资料库/idea库/01_机制与状态解耦.md)为准；它定义的是可被 LOCO 证伪的迁移假设，而非预设所有细胞系响应完全相同。

## 4. 数据策略：先获得解耦所需的证据

### 4.1 P0：外部训练数据获取

优先收集人源、单基因 knockdown/CRISPRi 的 Perturb-seq 数据，要求至少保存：

数据按证据等级使用：

| 等级 | 数据特征 | 用途 |
| --- | --- | --- |
| A | 多细胞系、相同或可比 KD、matched NTC、时间/协议明确 | 共享 GRN-ODE 的主要证据 |
| B | 单细胞系或 KD 覆盖有限，但质量和元数据可靠 | 扩展节点/干预覆盖；较低权重 |
| C | 协议、扰动类型或时间窗显著不同 | 仅用于候选边、预训练表示或独立敏感性分析 |

首要锚点是“相同 KD 在多个不同初始状态下的响应”。它不是要求各细胞系的响应相同，而是为估计条件效应 `tau_a(S)`、检验共享机制提供重叠证据。过表达、化学扰动和不同物种不直接混入 KD 主训练域；只有干预算子和协议差异可明确建模、并经过消融时才作为辅助证据。
CRISPRa数据也可以收集，作为第二证据梯队数据。不直接参与模型训练，只在后续参与模型评估以及加强训练。

需要通过文献，数据库等调研，尽可能收集可用的dataset（人源，CRISRPi单细胞优先，CRISPRa也可以用），并且尽可能详细地记录这些数据的信息（数据质量，对应细胞模型，实验手段，测序平台等）。每个dataset都应当有一份信息登记。最后还需要一份汇总报告。

### 4.2 配对、状态与协议

每个扰动条件必须匹配同一 `source study + cell line + batch/replicate + time` 的 NTC。Perturb-seq 不是同一细胞的前后追踪，训练目标是条件分布，不伪造单细胞配对轨迹。

协议/平台/批次作为观测尺度或 nuisance 协变量处理，不能被写入共享 GRN，也不能给每个细胞系自由学习一张网络。目标细胞系的身份不作为可记忆 embedding；它的状态信息只来自 NTC 分布。

### 4.3 训练前的重叠与因果校正前端

对每个实验环境 `e=(study, cell line, batch, replicate, time)`，从 matched NTC 得到预干预状态 `S_e`，并在 program 空间构造条件效应：

\[
d_{e,a}=\mathbb E[\psi(X_{e,a})]-\mathbb E[\psi(X_{e,0})].
\]

这一步先消去同环境中对 KD 和 NTC 都相同的加性基线/批次项；原始单细胞分布仍保留，不能把 `d` 误写成单细胞的伪配对轨迹。该差分默认是 `E1_assignment_itt` 的并行随机对照效应，不是经典 Difference-in-Differences，也不自动等于实际敲低剂量效应。对同一 KD `a`，只在初态 `S` 的共同支持域内比较不同环境；在每个 `S` strata 内，再按 S00 的 covariate-role 合同处理允许的预处理/设计变量。若为边证据汇总而标准化 `S`，目标分布必须预先定义，且标准化结果不能替换训练时的条件效应。每条训练证据都携带 `estimand_id, d, S, H_pre, weight, variance, support flag, selection/interference flags`，而不是输出一份被覆盖写掉的“无混杂表达矩阵”。

因果校正的独立单位是“有 matched NTC 的 KD 条件/重复”，不是其中的单个细胞。更多单细胞只会降低该条件响应分布及方差的估计误差，不能制造额外的跨环境 overlap，也不能被当作独立匹配样本。项目显式区分四层 estimand：`E1_assignment_itt`、`E2_knockdown_dose`、`E3_transport`、`E4_prediction_distribution`；E2/E3 不可识别时仍可由获批简单模型生成 E4，但不得倒推因果结论。

细胞系初态是条件效应修饰变量，不能被全局 batch correction 抹平；其作用是学习 `d_{e,a}=tau_a(S_e)+...`，不是要求 `tau_a(S)` 与 `S` 无关。未观测批次只有在对 KD/NTC 加性且共同作用时才由 matched NTC 差分消去。若存在 `KD x batch` 交互，必须依赖重复实验、负对照代理和敏感性分析；没有这些信息时不宣称已经校正。KD 后 QC、cell cycle、stress、凋亡、存活/捕获等变量默认按中介、选择变量或诊断量处理，不作为普通混杂变量回归掉。具体识别假设、层级汇总、可选 weighting/DR/IV 及敏感性门控以[跨环境重叠与因果校正](参考资料库/idea库/06_跨环境重叠与因果校正.md)为准。

## 5. 主模型

### 5.1 可解释状态空间

首版在 `K` 个稳定 gene programs 上建模，而不在全基因空间直接拟合大 ODE：

\[
z=W^T\tilde x,\qquad \hat x=D(z).
\]

优先使用 PCA、NMF/cNMF 或稀疏 gene-program 表示。`W` 在每个外层训练折中拟合，避免留出细胞系泄漏。目标细胞系的 NTC 细胞分别映射为多个初始点，从而保留输入分布，而非只保留一个均值。

关于 program 状态、初态分布和有限时间解码的具体边界，见[GRN-ODE 与有限时间边界](参考资料库/idea库/03_GRN_ODE与有限时间边界.md)。[CellNavi 方法卡](参考资料库/文献/CellNavi/方法卡.md)仅记录“图增强状态表示”的后备选择；首版仍以 PCA/NMF/cNMF 为默认，除非它在 LOCO 中带来可复现增益。

### 5.2 共享 GRN-ODE

首版使用允许反馈、稀疏且低秩的 program-level 动力学：

\[
\frac{dz}{dt}=A\,\sigma(Bz+b+u_a)-\Lambda z,
\qquad G_predictive=AB.
\]

- `G_predictive` 表示干预约束下的共享预测有效耦合，允许反馈环，不强行裁成 DAG；它不自动等于直接调控 GRN。
- `A,B,b,Lambda` 在训练细胞系间共享；
- 稀疏、低秩和数值稳定约束避免用参数容量记忆 `cell line x KD` 答案；
- 单终点数据仅支持从 NTC 到给定 `T` 的有限时间生成解释，不宣称恢复真实中间轨迹、长期吸引子或完整景观。

若有可比较的多时间点，才联合拟合整段 ODE，并做 time-held-out 与滚动预测。one-step transition 是同容量的基线或数值近似，用于检验 ODE 的连续结构是否确有增益，不与 GRN-ODE 并列为项目内核。

参数化、扰动输入和数值求解优先参考[PerturbODE 方法卡](参考资料库/文献/PerturbODE/方法卡.md)；图约束向量场、未见时间点评估与 MoE 仅在后续门控阶段参考[MEGA-ODE 方法卡](参考资料库/文献/MEGA-ODE/方法卡.md)。两者都不能替代本项目的共享 `G_predictive,theta` 与 NTC 初态训练组织。

### 5.3 Knockdown 是软干预

\[
u_a=-\rho_{c,a}q_a.
\]

`q_a` 将被敲低基因映射到 program/ODE 输入；`rho` 是 knockdown 强度。若测得效率则直接使用；否则仅在合理范围内作为受正则约束的训练变量或按实验层级估计。CRISPRi 不被当作 perfect knockout。

### 5.4 状态与机制如何解耦

训练时所有细胞系共用同一个 `f_(theta,G_predictive)`，而各自从自己的 matched NTC 得到 `z_c(0)`；训练前端提供的加权条件效应确保某些只在特定初态出现的 KD 不会支配共享预测图：

```text
相同 KD 在共同支持的多初态中可重复的响应结构 -> 约束共享 G_predictive、theta
不同 NTC 初始点导致的真实响应差异 -> 由 z_c(0) 和 tau_a(S) 解释
共享模型无法解释且可由 NTC 预测的稳定残差 -> 才允许小型 adapter
```

必要时只加入二阶段、低秩、低幅度且由 NTC 推断的残差：

\[
f_c(z,u)=f_{\theta,G_predictive}(z,u)+R_\phi(h_c;z,u),\qquad h_c=Pool(E(X_c^{NTC})).
\]

`R_phi` 默认关闭，只有在 leave-one-cell-line-out 稳定提升时才启用。禁止每个细胞系自由学习 `G_c`；否则新细胞系只有 NTC 时不可辨识，也破坏项目假设。

跨细胞系共享参数、状态交换检验和 adapter 启用门槛的完整定义见[机制与状态解耦](参考资料库/idea库/01_机制与状态解耦.md)。

## 6. GRN 如何构建：复用候选，数据定权

GRN 采用三层，而不是“全靠先验”或“从全连接图盲学”二选一：

```text
候选图 M：成熟 GRN 工具 + TF-target/pathway 资源提供候选边
预测耦合 G_predictive：跨细胞系 perturbation 响应在 GRN-ODE 训练中定方向、符号、强度
解释图：训练后为边附加外部证据，不反向覆盖数据结论
```

可复用 GENIE3、GRNBoost2、SCENIC/pySCENIC、Inferelator、CellOracle 等工具生成候选边；它们不直接等于最终因果 GRN。每条候选边记录：

```text
shared_score, sign_consistency, effect_size, context_interaction,
protocol_stability, prior_support
```

最终 `G_predictive` 由以下联合约束决定：扰动后分布拟合、被敲低基因方向一致性、跨细胞系共享风险、稀疏性和稳定性。单终点数据下，边应称作“干预约束的共享预测耦合/候选有效耦合”；独立保存 `interventional_effect_evidence`，不应把任一对象写成已证实的直接 TF-target 关系。

知识图只用于缩小候选空间、降低有独立证据边的稀疏惩罚、补充注释。必须比较无先验、真实先验与 degree-matched 随机图；若真实图未带来 OOD 增益，就移除图模块。

候选图、干预证据、预测耦合和解释图的职责及候选边字段见[跨细胞系 GRN 构建](参考资料库/idea库/02_跨细胞系_GRN构建.md)。知识先验的允许范围、外部表示的准入条件与随机图消融见[知识先验与现成方法复用](参考资料库/idea库/05_知识先验与现成方法复用.md)；其中[AROMA 方法卡](参考资料库/文献/AROMA/方法卡.md)和[TxPert 方法卡](参考资料库/文献/TxPert/方法卡.md)只提供知识证据/外部预测对照，不能进入 `G_predictive` 的定权。

## 7. 训练、验证与模型门控

总损失可写为：

\[
\mathcal L=\mathcal L_{distribution}^{weighted}
+\lambda_{KD}\mathcal L_{KD}
+\lambda_{bridge}\mathcal L_{bridge}
+\lambda_{sparse}\lVert G\rVert_1
+\lambda_{prior}\mathcal L_{soft\ prior}
+\lambda_{adapter}\lVert R_\phi\rVert^2.
\]

`distribution` 使用 Sinkhorn/MMD 或适合原始计数的 likelihood；最终选择取决于输出格式和内部验证。模型按以下顺序升级：

`L_bridge` 只作用于有共同支持、且完成 `S` 条件化与 `H` 平衡的重复 KD 证据；它检验共享 ODE 是否解释这些桥接环境中的条件效应。它不惩罚由 `S` 解释的真实响应异质性，也不在无重叠的环境间强行制造“不变性”。

| 阶段 | 模型 | 作用 |
| --- | --- | --- |
| P0 | 外部数据获取、许可/元数据/配对审计 | 建立跨细胞系解耦证据和内部验证折 |
| P1 | no-change、平均 delta、ridge/reduced-rank | 建立不可绕过的强基线 |
| P2 | 共享线性 GRN-ODE | 验证“共享机制 + 不同初态”是否被数据支持 |
| P3 | 共享 nonlinear GRN-ODE | 首个主模型交付 |
| P4 | 软知识先验 | 验证图是否提供真实增益 |
| P5 | 低秩 context adapter 或 MEGA-ODE 类 MoE | 仅在 P3/P4 的 LOCO 残差证明需要时启用 |

必须执行的内部验证：

| 验证 | 要回答的问题 |
| --- | --- |
| leave-one-cell-line-out | 只用留出细胞系 NTC 时，机制能否迁移？ |
| leave-one-perturbation-out | GRN 能否传播未见 KD，而非记忆 action？ |
| cell-line + perturbation 双留出 | 状态和机制是否能同时外推？ |
| state swap / context permutation | 预测是否依赖正确 NTC 状态？ |
| overlap / balance audit | 各 KD 的训练环境是否在初态和技术协变量上可比，权重是否稳定？ |
| matched-NTC / replicate placebo | 加性批次是否已由条件差分消去，是否仍有 KD x batch 交互？ |
| shared ODE vs free per-cell-line model | 共享假设是否足够，adapter 是否必要？ |
| 无图 vs 真图 vs 随机图 | 图是否有超出容量/度分布的贡献？ |
| ODE vs 同容量 one-step | 连续结构是否带来实际增益？ |

所有图阈值、基因筛选、表示拟合、早停和超参数仅在每个训练折内确定。指标按 cell line 和 perturbation 分层，至少包括 expression/delta 误差、相关性、DEG 方向、分布距离和方差校准。

基线顺序、复杂度升级规则及状态/先验消融的判据见[基线、验证与复杂度门控](参考资料库/idea库/04_基线_验证与复杂度门控.md)。数据切分、control matching 和分层评估按[scPerturBench 方法卡](参考资料库/文献/scPerturBench/方法卡.md)执行；强线性对照按[Linear-baselines 方法卡](参考资料库/文献/Linear-baselines/方法卡.md)执行。

## 8. 复用边界

| 资产 | 可复用 | 不可替代项目核心 |
| --- | --- | --- |
| [PerturbODE](参考资料库/文献/PerturbODE/方法卡.md) | 可解释 ODE、扰动输入和结构化动力学接口 | 必须改造成共享机制/NTC 初态/LOCO 任务结构 |
| [MEGA-ODE](参考资料库/文献/MEGA-ODE/方法卡.md) | 图约束 ODE、GAT 向量场、后续 MoE 思路 | 外部网络先验不能替代从 perturbation 数据学习 `G_predictive`；GPL-3.0 代码不直接复制 |
| 成熟 GRN 工具 | 候选边和初始化 | 不直接输出最终机制图 |
| [AROMA](参考资料库/文献/AROMA/方法卡.md) / [TxPert](参考资料库/文献/TxPert/方法卡.md) | 外部预测基线、知识证据、长尾基因参考 | 不作为 ODE 状态、直接因果边或共享预测耦合 |
| PCA/NMF/cNMF、成熟 ODE solver 与[CellNavi](参考资料库/文献/CellNavi/方法卡.md) | 状态空间和数值基础设施；CellNavi 仅作图增强表示备选 | 项目不从零实现这些通用组件；CellNavi 不进入首版 |

## 9. 首版项目边界

```text
主模型：跨细胞系共享、由 perturbation 数据学习的 predictive GRN-ODE
状态：目标细胞系 NTC 决定初始点
机制：共享 G_predictive、theta 决定预测响应规律
干预：KD gene 作为 soft intervention
知识图：候选边和软修正
复用：成熟 GRN 候选工具、PerturbODE/MEGA-ODE 方法组件和 ODE solver
自建：跨细胞系预测耦合训练、G_predictive 的数据定权、比赛推理组织
对照：no-change、mean-delta、ridge/reduced-rank、同容量 one-step、TxPert/AROMA
```

首版不建设通用数据接口、完整图平台、长期景观分析或自由 per-cell-line GRN。只有 P3 在严格内部 OOD 验证中优于强基线后，才扩展知识图、adapter 或 MoE。

## 10. 实施资料定位索引

以下索引是后续实现时的唯一导航：先读大纲中对应章节，再进入链接资料补足算法细节；资料中的替代方案不得越过本表的启用门槛而直接加入首版。

| 资料 | 在本路线中的唯一角色 | 对应大纲步骤 | 状态 |
| --- | --- | --- | --- |
| [01 机制与状态解耦](参考资料库/idea库/01_机制与状态解耦.md) | 共享 `G_predictive,theta` 与 NTC 初态的可证伪假设；adapter 门控 | 3、5.4、7 | 主线必读 |
| [02 跨细胞系 GRN 构建](参考资料库/idea库/02_跨细胞系_GRN构建.md) | 候选图 `M`、干预证据、预测耦合 `G_predictive`、解释图的分层及边证据字段 | 6 | 主线必读 |
| [03 GRN-ODE 与有限时间边界](参考资料库/idea库/03_GRN_ODE与有限时间边界.md) | program 表示、soft intervention 与单终点可解释范围 | 5.1--5.3 | 主线必读 |
| [04 基线、验证与复杂度门控](参考资料库/idea库/04_基线_验证与复杂度门控.md) | 基线阶梯、OOD 验证和模型升级条件 | 7 | 主线必读 |
| [05 知识先验与现成方法复用](参考资料库/idea库/05_知识先验与现成方法复用.md) | 知识图、预训练表示与现成预测器的复用边界 | 6、8 | 主线必读 |
| [06 跨环境重叠与因果校正](参考资料库/idea库/06_跨环境重叠与因果校正.md) | 训练前的 matched NTC 差分、共同支持、平衡与敏感性门控 | 4.2--4.3 | 主线必读 |
| [PerturbODE](参考资料库/文献/PerturbODE/方法卡.md) | ODE 参数化、扰动注入和求解器的实现参考 | 5.2--5.3、8 | 主线组件参考 |
| [MEGA-ODE](参考资料库/文献/MEGA-ODE/方法卡.md) | 图向量场和 MoE 的后备实现参考 | 5.2、7 P5、8 | 仅 P5；须先通过 LOCO 门控 |
| [scPerturBench](参考资料库/文献/scPerturBench/方法卡.md) | OOD 切分、control matching 和评测协议 | 7 | 主线评测参考 |
| [Linear-baselines](参考资料库/文献/Linear-baselines/方法卡.md) | 不可跳过的强线性基线 | 7 | 主线评测参考 |
| [AROMA](参考资料库/文献/AROMA/方法卡.md) | 知识证据和外部预测对照 | 6、8 | 对照/软先验；不进内核 |
| [TxPert](参考资料库/文献/TxPert/方法卡.md) | 知识图 OOD 对照 | 6、8 | 对照；不进内核 |
| [CellNavi](参考资料库/文献/CellNavi/方法卡.md) | 图增强状态表示的备选 | 5.1、8 | 默认不用；需 LOCO 优于 PCA/NMF |
| [因果重叠校正](参考资料库/文献/因果重叠校正/方法卡.md) | 前端校正工具的假设、算法选型和禁止事项 | 4.3 | 主线方法参考 |

每篇本地论文 PDF 均由其对应的方法卡链接；没有本地 PDF 的条目保留其官方论文入口。当前资料库中不存在“未被本大纲链接且没有作用”的 idea 或文献条目。
